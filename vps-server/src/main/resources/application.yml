spring:
  application:
    name: vps-server
  datasource:
    url: jdbc:h2:file:../vpsdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      data-locations: classpath:data.sql
  security:
    user:
      name: admin
      password: admin
  # 邮件配置
  mail:
    host: smtp.163.com
    username: commonutil@163.com
    password: XMaUr3BBZFKCdJBw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.163.com

jwt:
  secret: mySecretKey123456789012345678901234567890
  expiration: 0 # 永不过期

server:
  port: 8080

logging:
  level:
    com.vps: DEBUG